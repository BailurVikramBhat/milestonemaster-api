app:
  cors:
    allowed-origins:
      - http://localhost:3000
      - http://localhost:5173
      - http://localhost:8081
  qa-seed:
    email: ${QA_SEED_EMAIL:qa@milestonemaster.com}
    password: ${QA_SEED_PASSWORD:Simple123@}
    full-name: ${QA_SEED_FULL_NAME:QA User}
  auth:
    cookie:
      secure: false
      same-site: Lax

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5433/milestonemaster_test}
    username: ${DB_USERNAME:milestonemaster_test}
    password: ${DB_PASSWORD:milestonemaster_test}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: ${DDL_AUTO:update}
    show-sql: false
    open-in-view: false

logging:
  level:
    com.vikrambhat.milestonemaster: INFO
    org.springframework.security: WARN
    org.hibernate.SQL: WARN
    org.hibernate.orm.jdbc.bind: WARN
